This package contains timidity configuration files and sound founts, to install in SafaOS, these files must be copied to `sys:/` as unix `/` maps to it, there is currently no SafaOS patches to timidity so that our paths would work.

While building an ISO using the `safa-helper`, you can copy these files directly to `ramdisk-include` and they will be bundled with the OS.
